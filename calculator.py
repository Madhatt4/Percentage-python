def calculate_percentage(total, percentage):
    """
    Calculate the percentage of a given number.

    :param total: the base number to calculate the percentage of
    :param percentage: the percentage to calculate
    :return: the calculated percentage of the total
    """
    return (total * percentage) / 100
